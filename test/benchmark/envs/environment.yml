name: iGenVar_benchmark
channels:
  - defaults
  - conda-forge
  - bioconda
dependencies:
  - bgzip # rule tabix - accepts only zipped files zipped with bgzip
  - mamba
  - pip
  - python=3.6
  - r
  - r-tidyverse
  - samtools
  - snakemake
  - tabix
  - yappi
  - pip:
    - truvari
